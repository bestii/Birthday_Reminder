# 11 — Search, filter & sort on Home

**What to build:** Home header search (by person name), filter panel (sort Date/A–Z + event-type filter), and group pills — all composing to filter the list, resetting to defaults (group All, sort Date, events All) on every app open.

**Blocked by:** 07 — Month-grouped birthday cards

**Status:** ready-for-agent

- [ ] Search icon opens an input that filters the people list live by name.
- [ ] Filter panel offers sort (Date / A–Z) and event-type (All / Birthday / Anniversary / Memorial / custom).
- [ ] Group pills filter the list by group.
- [ ] Search, type, and group filters compose correctly.
- [ ] Filter state resets to defaults on every app open (not persisted).
- [ ] Calendar is unaffected by Home filters.
