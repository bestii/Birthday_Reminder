# 09 — Add event & custom event types

**What to build:** The FAB "Add Event" flow — pick an existing person, choose an event type (Anniversary / Memorial / custom) or create a custom type inline, set date and notes, and save an additional event for that person.

**Blocked by:** 08 — Person detail & event edit

**Status:** ready-for-agent

- [ ] "Add Event" opens a person picker.
- [ ] Event-type selection offers built-ins plus custom types.
- [ ] A custom event type can be created inline (name only) and persists.
- [ ] Date (month/day, optional year) and notes fields are present.
- [ ] Save adds the event to the person and it appears in the detail view.
