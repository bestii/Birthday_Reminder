# 10 — Calendar screen

**What to build:** The Calendar tab — monthly grid with prev/next nav, today marked with a ring, dates with events marked with a uniform neutral dot, tapping a date lists that day's event cards below (avatar, name, meta, type chip), an add-event `+` beside the date label, and a floating `+` FAB.

**Blocked by:** 07 — Month-grouped birthday cards

**Status:** ready-for-agent

- [ ] Monthly calendar renders with prev/next navigation.
- [ ] Today is shown with a ring; the selected day is filled distinctly.
- [ ] Dates with events show a uniform neutral dot (no per-type coloring).
- [ ] Tapping a date lists that day's events below (cards with avatar/name/meta/type chip).
- [ ] Add-event `+` beside the date label opens the add-event flow.
- [ ] Calendar shows all events, with no filtering.
